import { CustomNodeLayout } from "../CustomNodeLayout";

const CustomInputNode = (props) => {

  return (
    <>
      <CustomNodeLayout
        nodeProps={props}
      />
    </>
  );
};
export default CustomInputNode;
